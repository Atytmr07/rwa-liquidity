"""Data files shipped with the package.

This is a package rather than a bare directory so that the files can be read
through `importlib.resources`, which works whether the package is installed from
a wheel, from a zip, or in editable mode from a source tree.
"""
